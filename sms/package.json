{
  "name": "sms",
  "version": "1.0.0",
  "description": "Fetch and manipulate JSON data from a fictional Social Network REST API.",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "apiserver": "json-server --watch --port 4000 api/db.json --routes api/routes.json"
  },
  "author": "Miguel Ângelo",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.16.1"
  }
}
